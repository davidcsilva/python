distancia = float(input('Digite a distancia a percorrer em km: '))
velocidade = float(input('Digite a velocidade media em km/h: '))

tempo = distancia/velocidade

print('O tempo de viagem é %0.2f horas' % tempo)